default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

  #以下追加
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  database: bird_app_development

test:
  <<: *default
  database: bird_app_test
